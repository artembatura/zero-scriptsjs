name: Node.js CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: pnpm/action-setup@v1.2.0
        with:
          version: latest
      - run: pnpm i
      - run: pnpm compile
      - run: pnpm test
        env:
          CI: true